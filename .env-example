# API Configuration
# URL completa da API em produção (ex: https://api.seudominio.com)
VITE_API_URL=https://api.seudominio.com

# URL base da API (usado para OAuth2/Google Login)
# Geralmente é a mesma que VITE_API_URL
VITE_API_BASE_URL=https://api.seudominio.com

# Google Maps API Key
# Necessário para exibir mapas e obter direções
VITE_GOOGLE_MAPS_API_KEY=sua_chave_google_maps_aqui

# Base URL da aplicação (opcional)
# Use apenas se a aplicação não estiver na raiz do domínio
# Exemplo: se estiver em https://seudominio.com/app, use: BASE_URL=/app
BASE_URL=/

